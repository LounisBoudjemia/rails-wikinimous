<body>
</button>
  <div class="container mt-3">
    <div class="row">
      <div class="col-sm-8 offset-sm-2">
          <article class="mb-3">
            <% @articles.each do |article| %>
              <h2 class="title"><%= link_to  article.title, article_path(article.id) %></h2>
              <p class="content"><%= article.content %></p>
            </article>
            <% end %>
      </div>
    </div>
  </div>
  </nav>
</body>
